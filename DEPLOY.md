# Deploy Trimestt v23

Place `trimestt-app-v23.zip` in Downloads beside the existing
`trimestt-app-v5` repository, then run the commands provided with this release.

The upgrade replaces the browser files and version metadata only. Existing
hospital data, uploads, environment variables and server configuration remain
in the current repository. Run the bundled 669-check test suite before pushing.
